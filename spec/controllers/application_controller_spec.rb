# require 'rails_helper'

# RSpec.describe ApplicationController, :type => :controller do

# 	it "Check that authorize does not work when there is no current user" do 
		
# 		get :authorize 

# 		expect(response).to redirect_to(application_layout_path)
# 	end

# end

# * Check that authorize does not work when there is no current user
# * Check that authrorize does work when there is a valid user. 